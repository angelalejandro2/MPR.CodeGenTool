// Autogenerated code - Do not modify directly
// You can customize this by creating a partial class in the same namespace
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using AutoMapper;
using {{ entity_namespace }};
using {{ repository_interface_namespace }};
using {{ dto_namespace }};

namespace {{ namespace }}
{
    public partial class {{ name }}
    {
        private readonly IUnitOfWork _unitOfWork;
        private readonly IMapper _mapper;
        
        public {{ name }}(IUnitOfWork unitOfWork, IMapper mapper)
        {
            _unitOfWork = unitOfWork;
            _mapper = mapper;
        }
        
        {{~ if !is_keyless ~}}
        public virtual async Task<IEnumerable<{{ dto_name }}>> GetAllAsync()
        {
            var entities = await _unitOfWork.{{ entity_name }}Repository.GetAllAsync();
            return _mapper.Map<IEnumerable<{{ dto_name }}>>(entities);
        }
        
        {{~ if has_composite_key ~}}
        public virtual async Task<{{ dto_name }}> GetByIdAsync(
            {{~ for prop in key_properties ~}}
            {{ prop.type }} {{ prop.camel_case_name }}{{ if !for.last }}, {{ end }}
            {{~ end ~}}
        )
        {
            var entity = await _unitOfWork.{{ entity_name }}Repository.GetByIdAsync(new object[] { 
                {{~ for prop in key_properties ~}}
                {{ prop.camel_case_name }}{{ if !for.last }}, {{ end }}
                {{~ end ~}}
            });
            
            return _mapper.Map<{{ dto_name }}>(entity);
        }
        {{~ else ~}}
        public virtual async Task<{{ dto_name }}> GetByIdAsync({{ key_properties[0].type }} id)
        {
            var entity = await _unitOfWork.{{ entity_name }}Repository.GetByIdAsync(id);
            return _mapper.Map<{{ dto_name }}>(entity);
        }
        {{~ end ~}}
        
        public virtual async Task<{{ dto_name }}> AddAsync({{ create_dto_name }} createDto)
        {
            var entity = _mapper.Map<{{ entity_name }}>(createDto);
            var addedEntity = await _unitOfWork.{{ entity_name }}Repository.AddAsync(entity);
            await _unitOfWork.SaveChangesAsync();
            
            return _mapper.Map<{{ dto_name }}>(addedEntity);
        }
        
        {{~ if has_composite_key ~}}
        public virtual async Task<{{ dto_name }}> UpdateAsync({{ update_dto_name }} updateDto)
        {
            var entity = _mapper.Map<{{ entity_name }}>(updateDto);
            var updatedEntity = await _unitOfWork.{{ entity_name }}Repository.UpdateAsync(entity);
            await _unitOfWork.SaveChangesAsync();
            
            return _mapper.Map<{{ dto_name }}>(updatedEntity);
        }
        
        public virtual async Task DeleteAsync(
            {{~ for prop in key_properties ~}}
            {{ prop.type }} {{ prop.camel_case_name }}{{ if !for.last }}, {{ end }}
            {{~ end ~}}
        )
        {
            await _unitOfWork.{{ entity_name }}Repository.DeleteAsync(new object[] { 
                {{~ for prop in key_properties ~}}
                {{ prop.camel_case_name }}{{ if !for.last }}, {{ end }}
                {{~ end ~}}
            });
            
            await _unitOfWork.SaveChangesAsync();
        }
        {{~ else ~}}
        public virtual async Task<{{ dto_name }}> UpdateAsync({{ update_dto_name }} updateDto)
        {
            var entity = _mapper.Map<{{ entity_name }}>(updateDto);
            var updatedEntity = await _unitOfWork.{{ entity_name }}Repository.UpdateAsync(entity);
            await _unitOfWork.SaveChangesAsync();
            
            return _mapper.Map<{{ dto_name }}>(updatedEntity);
        }
        
        public virtual async Task DeleteAsync({{ key_properties[0].type }} id)
        {
            await _unitOfWork.{{ entity_name }}Repository.DeleteAsync(id);
            await _unitOfWork.SaveChangesAsync();
        }
        {{~ end ~}}
        {{~ end ~}}
    }
}