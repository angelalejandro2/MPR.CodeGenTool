// <auto-generated />
using MediatR;
using Microsoft.AspNetCore.Mvc;
using System.Threading.Tasks;
using {{ solutionName }}.Application.Commands;
using {{ solutionName }}.Application.Queries;
using {{ solutionName }}.Application.Dtos;

namespace {{ solutionName }}.Controllers
{
    [ApiController]
    [Route("api/[controller]")]
    public class {{ entity.name }}Controller : ControllerBase
    {
        private readonly IMediator _mediator;

        public {{ entity.name }}Controller(IMediator mediator)
        {
            _mediator = mediator;
        }

        [HttpGet]
        public async Task<IActionResult> Get()
        {
            var result = await _mediator.Send(new Get{{ entity.name }}ListQuery());
            return Ok(result);
        }
        {{~ if entity.primaryKeys.size > 0 ~}}
        {{~ if entity.primaryKeys.size == 1 ~}}

        [HttpGet("{id}")]
        public async Task<IActionResult> GetById({{ entity.primaryKeys[0].type }} id)
        {
            var result = await _mediator.Send(new Get{{ entity.name }}ByIdQuery { {{ entity.primaryKeys[0].name }} = id });
            if (result == null)
                return NotFound();
            return Ok(result);
        }

        [HttpPut("{id}")]
        public async Task<IActionResult> Update({{ entity.primaryKeys[0].type }} id, [FromBody] Update{{ entity.name }}Command command)
        {
            if (id != command.{{ entity.primaryKeys[0].name }})
                return BadRequest();

            var result = await _mediator.Send(command);
            return Ok(result);
        }

        [HttpDelete("{id}")]
        public async Task<IActionResult> Delete({{ entity.primaryKeys[0].type }} id)
        {
            var result = await _mediator.Send(new Delete{{ entity.name }}Command { {{ entity.primaryKeys[0].name }} = id });
            return Ok(result);
        }

        {{~ else ~}}

        [HttpGet("by-id")]
        public async Task<IActionResult> GetById([FromQuery] Get{{ entity.name }}ByIdQuery query)
        {
            var result = await _mediator.Send(query);
            if (result == null)
                return NotFound();
            return Ok(result);
        }

        [HttpPut("by-id")]
        public async Task<IActionResult> Update([FromBody] Update{{ entity.name }}Command command)
        {
            var result = await _mediator.Send(command);
            return Ok(result);
        }

        [HttpDelete("by-id")]
        public async Task<IActionResult> Delete([FromBody] Delete{{ entity.name }}Command command)
        {
            var result = await _mediator.Send(command);
            return Ok(result);
        }

        {{~ end ~}}

        [HttpPost]
        public async Task<IActionResult> Create([FromBody] Create{{ entity.name }}Command command)
        {
            var result = await _mediator.Send(command);
            return Ok(result);
        }

        {{~ end ~}}
    }
}