// <auto-generated />
// This file was generated by MPR.CodeGenTool on {{ now | date.to_string "yyyy-MM-dd HH:mm" }}

using AutoMapper;
using MediatR;
using Microsoft.Extensions.Logging;
using {{ solutionName }}.Application.Dtos.Queries;
using {{ solutionName }}.Domain.Repositories;

namespace {{ solutionName }}.Application.{{ entity.name }}.Queries
{
    public class Get{{ entity.name }}ByIdQueryHandler : IRequestHandler<Get{{ entity.name }}ByIdQuery, {{ entity.name }}Dto?>
    {
        private readonly I{{ entity.name }}Repository _repository;
        private readonly IMapper _mapper;
        private readonly ILogger<Get{{ entity.name }}ByIdQueryHandler> _logger;

        public Get{{ entity.name }}ByIdQueryHandler(
            I{{ entity.name }}Repository repository,
            IMapper mapper,
            ILogger<Get{{ entity.name }}ByIdQueryHandler> logger)
        {
            _repository = repository;
            _mapper = mapper;
            _logger = logger;
        }

        public async Task<{{ entity.name }}Dto?> Handle(Get{{ entity.name }}ByIdQuery request, CancellationToken cancellationToken)
        {
            var entity = await _repository.GetByIdAsync(new object[] { {{ entity.keys | array.map 'k' => 'request.' + k.name | string.join ", " }} }, cancellationToken);
            return entity is null ? null : _mapper.Map<{{ entity.name }}Dto>(entity);
        }
    }
}