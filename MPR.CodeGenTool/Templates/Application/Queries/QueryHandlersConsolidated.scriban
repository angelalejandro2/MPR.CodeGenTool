// <auto-generated />
// This file was generated by MPR.CodeGenTool on {{ now | date.to_string "yyyy-MM-dd" }}

using MediatR;
using AutoMapper;
using {{ solutionName }}.Domain.Interfaces;
using {{ solutionName }}.Application.Dtos.Queries;

namespace {{ solutionName }}.Application.Queries
{
    public class Get{{ entity.name }}ByIdQueryHandler : IRequestHandler<Get{{ entity.name }}ByIdQuery, {{ entity.name }}Dto>
    {
        private readonly IUnitOfWork _unitOfWork;
        private readonly IMapper _mapper;

        public Get{{ entity.name }}ByIdQueryHandler(IUnitOfWork unitOfWork, IMapper mapper)
        {
            _unitOfWork = unitOfWork;
            _mapper = mapper;
        }

        public async Task<{{ entity.name }}Dto> Handle(Get{{ entity.name }}ByIdQuery request, CancellationToken cancellationToken)
        {
            var entity = await _unitOfWork.{{ entity.name }}Repository.GetByIdAsync(
                {{ entity.primaryKeys | array.map 'k' => "request." + k.name | string.join ", " }},
                cancellationToken
            );

            return entity == null ? null : _mapper.Map<{{ entity.name }}Dto>(entity);
        }
    }

    public class Get{{ entity.name }}ListQueryHandler : IRequestHandler<Get{{ entity.name }}ListQuery, IEnumerable<{{ entity.name }}Dto>>
    {
        private readonly IUnitOfWork _unitOfWork;
        private readonly IMapper _mapper;

        public Get{{ entity.name }}ListQueryHandler(IUnitOfWork unitOfWork, IMapper mapper)
        {
            _unitOfWork = unitOfWork;
            _mapper = mapper;
        }

        public async Task<IEnumerable<{{ entity.name }}Dto>> Handle(Get{{ entity.name }}ListQuery request, CancellationToken cancellationToken)
        {
            var list = await _unitOfWork.{{ entity.name }}Repository.GetAllAsync(cancellationToken);
            return _mapper.Map<IEnumerable<{{ entity.name }}Dto>>(list);
        }
    }
}